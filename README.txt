StocksLSTMwTensorflow:

LSTM model to predict the next 30 days of any stock.

STEP 1: download any stocks .csv file from yahoo finance

STEP 2: in cell 2, insert the name of the CSV file

STEP 3: select run all cells


StocksLSTMwTensorflow:

this is the same program but without robustscaler toolbox.
It has some errors that still need to be worked out.
	LSTM model complains about the data that is being fed into 
	the input layer.